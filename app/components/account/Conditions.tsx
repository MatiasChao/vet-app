import React, {useRef} from 'react';
import {StyleSheet, View, Text} from 'react-native';

export default function Conditions() {
  const toastRef = useRef();

  return (
    <View>
        <Text> Agregar las condiciones </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  
});
